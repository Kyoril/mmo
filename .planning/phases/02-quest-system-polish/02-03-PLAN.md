---
phase: 02-quest-system-polish
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/shared/game_server/objects/game_player_s.cpp
  - src/mmo_client/systems/quest_client.cpp
  - src/mmo_client/systems/quest_client.h
autonomous: true
requirements:
  - QUEST-04
  - QUEST-05
  - QUEST-06

must_haves:
  truths:
    - "Quest objectives track kill counts and item collection in real-time"
    - "Quest chains function end-to-end — completing one unlocks the next"
    - "Turn-in rewards are applied correctly (items, XP, gold, spells)"
    - "Quest XP reward is displayed to the client upon completion"
  artifacts:
    - path: "src/shared/game_server/objects/game_player_s.cpp"
      provides: "Item credit tracking with quest completion check"
    - path: "src/mmo_client/systems/quest_client.cpp"
      provides: "XP display in quest details, reward completion UI event"
  key_links:
    - from: "GamePlayerS::OnQuestItemAddedCredit"
      to: "FulfillsQuestRequirements + quest completion"
      via: "inventory item add event → quest requirement check"
      pattern: "OnQuestItemAddedCredit"
    - from: "QuestClient::OnQuestGiverQuestDetails"
      to: "QuestDetails rewardXp"
      via: "packet read of XP field"
      pattern: "rewardXp"
---

<objective>
Verify and fix the end-to-end quest flow: objective tracking (kills + items), quest completion detection, reward XP display, and chain quest progression. This plan integrates the server-side fixes from Plan 01 with the client-side data layer from Plan 02 and closes remaining gaps.

Purpose: With Plan 01 fixing server reward logic and Plan 02 fixing client data parsing, this plan verifies the complete flow works and fixes any remaining integration gaps — particularly item credit tracking, XP display in quest details, and chain quest unlocking.

Output: Fully tested end-to-end quest flow.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quest-system-polish/02-01-SUMMARY.md
@.planning/phases/02-quest-system-polish/02-02-SUMMARY.md
@src/shared/game_server/objects/game_player_s.cpp
@src/shared/game_server/objects/game_player_s.h
@src/mmo_client/systems/quest_client.cpp
@src/mmo_client/systems/quest_client.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix item credit quest objective tracking</name>
  <files>src/shared/game_server/objects/game_player_s.cpp</files>
  <read_first>
    - src/shared/game_server/objects/game_player_s.cpp (OnQuestItemAddedCredit around line 1050-1100, OnQuestItemRemovedCredit, OnQuestSpellCastCredit)
    - src/shared/game_server/objects/game_player_s.h (OnQuestItemAddedCredit, OnQuestItemRemovedCredit signatures)
    - src/shared/game_server/objects/game_player_s.cpp (FulfillsQuestRequirements around line 940 — item requirements check)
  </read_first>
  <action>
1. **Read OnQuestItemAddedCredit** — verify it:
   - Iterates all quest log slots
   - For each active quest, checks if the added item matches a `req.itemid()` requirement
   - Fires `m_netPlayerWatcher->OnQuestItemCredit(...)` to notify client
   - Calls `FulfillsQuestRequirements` to check if quest is now complete
   - If complete, updates status to `quest_status::Complete` and fires `OnQuestDataChanged`

2. **Read OnQuestItemRemovedCredit** — verify it:
   - Checks if removing an item causes a previously complete quest to become incomplete again
   - Updates status back to `quest_status::Incomplete` if requirements no longer fulfilled

3. **Read OnQuestSpellCastCredit** — verify it handles the `spellcast` requirement field from proto.

4. **Fix any gaps found.** Common issues:
   - Missing `OnQuestItemCredit` watcher notification (item name, count, maxCount)
   - Missing completion check after item credit
   - Not updating quest log field counters for item requirements (items use inventory count, not counter fields — but the quest status still needs updating)

5. **Verify FulfillsQuestRequirements handles all requirement types**: creature kills (counter-based), items (inventory-count-based), spell casts, exploration flag. If any type is missing, add it.
  </action>
  <verify>
    <automated>grep -n "OnQuestItemAddedCredit\|OnQuestItemRemovedCredit\|OnQuestSpellCastCredit" src/shared/game_server/objects/game_player_s.cpp | head -15</automated>
  </verify>
  <acceptance_criteria>
    - OnQuestItemAddedCredit notifies client via OnQuestItemCredit watcher
    - OnQuestItemAddedCredit checks FulfillsQuestRequirements after credit
    - OnQuestItemRemovedCredit reverts quest status if requirements no longer met
    - FulfillsQuestRequirements handles creature, item, and spellcast requirements
  </acceptance_criteria>
  <done>Item collection quest objectives track correctly and trigger quest completion when all requirements are met.</done>
</task>

<task type="auto">
  <name>Task 2: Verify quest XP display and reward flow integration</name>
  <files>src/mmo_client/systems/quest_client.cpp, src/mmo_client/systems/quest_client.h</files>
  <read_first>
    - src/mmo_client/systems/quest_client.cpp (OnQuestGiverQuestDetails — check if rewardXp is parsed from packet)
    - src/mmo_client/systems/quest_client.cpp (OnQuestGiverOfferReward — check what reward info is shown)
    - src/mmo_client/systems/quest_client.cpp (OnQuestGiverQuestComplete — check QUEST_REWARDED event data)
    - src/mmo_client/systems/quest_client.h (QuestDetails struct — rewardXp field)
  </read_first>
  <action>
1. **Check OnQuestGiverQuestDetails XP parsing**: The current code reads `rewardMoney` and `rewardSpellId` from the packet but may not read `rewardXp`. Check if the server-side packet writer includes XP. If the server sends XP in the details packet, ensure the client reads it into `m_questDetails.rewardXp`. If the server doesn't send XP in details (only in the complete/reward packet), note this and ensure the reward completion event includes it.

2. **Check OnQuestGiverOfferReward**: This is the "turn in" dialog — verify it parses enough data for the UI to show what the player will receive. Currently it reads title and offerRewardText. Check if it should also read reward items and XP. If the server sends these, parse them.

3. **Verify QUEST_REWARDED event includes XP and money**: `OnQuestGiverQuestComplete` fires `QUEST_REWARDED` with `(title, rewardXp, rewardMoney)`. Verify these values are correctly read from the packet.

4. **Add any missing Lua-accessible reward data**: If `m_questDetails.rewardXp` is never populated from the details packet, add a Lua binding `GetQuestRewardXP()` that returns the value. Same for `GetQuestRewardMoney()` if missing.

Fix any gaps discovered. The goal is that the Lua UI can display all reward information (items, XP, gold, spell) both in the quest offer dialog AND the reward completion notification.
  </action>
  <verify>
    <automated>grep -n "rewardXp\|rewardMoney\|QUEST_REWARDED\|QUEST_DETAIL\|QUEST_OFFER" src/mmo_client/systems/quest_client.cpp | head -20</automated>
  </verify>
  <acceptance_criteria>
    - QuestDetails rewardXp is populated (either from details packet or from offer reward packet)
    - QUEST_REWARDED event includes title, XP, and money values
    - Lua scripts can access quest reward XP via GetQuestDetails or dedicated function
    - OnQuestGiverOfferReward fires QUEST_OFFER_REWARDS event with enough data for UI
  </acceptance_criteria>
  <done>Quest reward information (XP, gold, items, spells) is fully available to client UI at all quest interaction stages.</done>
</task>

</tasks>

<verification>
- Build the project: `cmake --build build`
- All quest objective types (kill, item, spell cast, exploration) track correctly
- Quest chain: accepting quest B requires quest A to be rewarded — verified by prevquestid logic
- Reward display: XP, money, items, spell all visible in client events
</verification>

<success_criteria>
End-to-end quest flow works: discover quest giver (!) → read details → accept → track objectives in real-time → complete → turn in → receive rewards (items, XP, gold, spell) → next chain quest becomes available (?→!).
</success_criteria>

<output>
After completion, create `.planning/phases/02-quest-system-polish/02-03-SUMMARY.md`
</output>
