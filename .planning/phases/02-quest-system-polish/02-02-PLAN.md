---
phase: 02-quest-system-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mmo_client/systems/quest_client.h
  - src/mmo_client/systems/quest_client.cpp
  - src/mmo_client/game_states/world_state.cpp
autonomous: true
requirements:
  - QUEST-01
  - QUEST-02
  - QUEST-03

must_haves:
  truths:
    - "Quest giver NPCs display correct status icons (!, ?) based on quest availability"
    - "Player can read quest description, objectives, and rewards before accepting"
    - "Quest log UI shows active quests with objectives and real-time progress"
    - "Quest reward items are stored and accessible to UI Lua scripts"
  artifacts:
    - path: "src/mmo_client/systems/quest_client.h"
      provides: "Reward item storage in QuestDetails, objective text in quest log"
    - path: "src/mmo_client/systems/quest_client.cpp"
      provides: "Complete reward item parsing, quest log objective text caching"
  key_links:
    - from: "QuestClient::OnQuestGiverQuestDetails"
      to: "QuestDetails reward items"
      via: "packet parsing into m_questDetails reward item lists"
      pattern: "rewardItems|rewardItemsChoice"
    - from: "QuestClient::UpdateQuestLog"
      to: "QUEST_LOG_UPDATE event"
      via: "FrameManager TriggerLuaEvent"
      pattern: "QUEST_LOG_UPDATE"
---

<objective>
Polish the client-side quest UI data layer: ensure reward items are fully parsed and stored for UI display, quest log objective text is properly cached and exposed to Lua, and quest giver status icon refresh is robust.

Purpose: The client already handles quest details packets and quest log updates, but reward items in QuestDetails are partially parsed (read but not stored for Lua access). Quest log objective text caching exists but needs verification. Quest giver icons work but need to refresh reliably on all state changes.

Output: Complete client-side quest data layer that Lua UI scripts can query for all quest information.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/mmo_client/systems/quest_client.h
@src/mmo_client/systems/quest_client.cpp
@src/shared/game/quest_info.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store reward items in QuestDetails for UI access</name>
  <files>src/mmo_client/systems/quest_client.h, src/mmo_client/systems/quest_client.cpp</files>
  <read_first>
    - src/mmo_client/systems/quest_client.h (QuestDetails struct around line 39-62)
    - src/mmo_client/systems/quest_client.cpp (OnQuestGiverQuestDetails around line 620-720, specifically the reward item parsing)
    - src/shared/game/quest_info.h (QuestRewardItem struct)
  </read_first>
  <action>
1. **Add reward item storage to QuestDetails** (in quest_client.h):
   Add these members to the `QuestDetails` struct:
   ```
   struct QuestRewardItemDisplay
   {
       uint32 itemId = 0;
       uint32 count = 0;
       uint32 displayId = 0;
   };
   std::vector<QuestRewardItemDisplay> rewardItemsChoice;
   std::vector<QuestRewardItemDisplay> rewardItems;
   ```
   Update `Clear()` to also clear these vectors.

2. **Parse ALL reward items in OnQuestGiverQuestDetails** (in quest_client.cpp):
   Currently the method reads only the first reward item and discards the rest. Change the parsing loop to:
   - Read `rewardItemsChoiceCount`, then loop `rewardItemsChoiceCount` times, reading each `(itemId, count, displayId)` triple and pushing to `m_questDetails.rewardItemsChoice`.
   - Read `rewardItemsCount`, then loop `rewardItemsCount` times, reading each `(itemId, count, displayId)` triple and pushing to `m_questDetails.rewardItems`.
   - Keep the existing `m_itemCache.Get(itemId)` call for each item.

3. **Expose reward items to Lua** (in RegisterScriptFunctions):
   Add luabind bindings for `QuestRewardItemDisplay` class with readonly properties `itemId`, `count`, `displayId`.
   Add lambda functions:
   - `GetQuestRewardItemCount` → returns `m_questDetails.rewardItems.size()`
   - `GetQuestRewardItem(index)` → returns pointer to `m_questDetails.rewardItems[index]`
   - `GetQuestRewardChoiceItemCount` → returns `m_questDetails.rewardItemsChoice.size()`
   - `GetQuestRewardChoiceItem(index)` → returns pointer to `m_questDetails.rewardItemsChoice[index]`

Follow existing Lua binding patterns in RegisterScriptFunctions (use `luabind::def_lambda` and `luabind::class_<>`).
  </action>
  <verify>
    <automated>grep -n "rewardItems\|QuestRewardItemDisplay\|GetQuestRewardItem" src/mmo_client/systems/quest_client.h src/mmo_client/systems/quest_client.cpp</automated>
  </verify>
  <acceptance_criteria>
    - quest_client.h contains QuestRewardItemDisplay struct with itemId, count, displayId
    - quest_client.h QuestDetails has rewardItemsChoice and rewardItems vectors
    - quest_client.cpp OnQuestGiverQuestDetails loops over all reward items (not just the first)
    - quest_client.cpp RegisterScriptFunctions has GetQuestRewardItemCount and GetQuestRewardItem bindings
    - QuestDetails::Clear() clears rewardItemsChoice and rewardItems
  </acceptance_criteria>
  <done>Reward items are fully parsed from server packets and exposed to Lua for UI display.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix quest log objective text and quest giver status refresh</name>
  <files>src/mmo_client/systems/quest_client.cpp</files>
  <read_first>
    - src/mmo_client/systems/quest_client.cpp (QuestLogSelectQuest, GetQuestObjectiveCount, GetQuestObjectiveText around line 400-440)
    - src/mmo_client/systems/quest_client.cpp (RefreshQuestGiverStatus around line 445)
    - src/mmo_client/systems/quest_client.cpp (OnQuestUpdate handler around line 770-860)
  </read_first>
  <action>
1. **Verify QuestLogSelectQuest populates objective texts correctly**:
   Read `QuestLogSelectQuest` — it should build `m_questObjectiveTexts` from the selected quest's requirements, showing progress like "Kill 3/5 Wolves". If the format is missing or incorrect:
   - For creature requirements: build text as "{creature_name}: {current}/{required}" using the creature cache
   - For item requirements: build text as "{item_name}: {current}/{required}" using the item cache
   - Current counts come from `m_questLog[slot].counters[reqIndex]`

2. **Ensure quest giver status refreshes on quest accept and abandon**:
   In `OnQuestAccepted` handler, verify `RefreshQuestGiverStatus()` is called (it should be — quest log update triggers it). In `OnQuestAbandoned`, verify the same. If either is missing, add the call.

3. **Ensure OnQuestUpdate (kill/item credit) triggers quest log refresh**:
   The `OnQuestUpdate` handler fires `UI_INFO_MESSAGE` events but should also trigger `QUEST_LOG_UPDATE` if the quest log data changed. Check if `UpdateQuestLog` is called or if `QUEST_LOG_UPDATE` is fired after credit updates. The quest log fields are updated server-side via object field replication, which triggers `UpdateQuestLog` in the field update handler — verify this chain works.
  </action>
  <verify>
    <automated>grep -n "RefreshQuestGiverStatus\|QUEST_LOG_UPDATE\|m_questObjectiveTexts" src/mmo_client/systems/quest_client.cpp</automated>
  </verify>
  <acceptance_criteria>
    - QuestLogSelectQuest builds m_questObjectiveTexts with creature/item names and progress counts
    - RefreshQuestGiverStatus is called after quest accept and abandon
    - QUEST_LOG_UPDATE event fires when quest objective progress changes
  </acceptance_criteria>
  <done>Quest log displays objectives with real-time progress. Quest giver icons update on all quest state changes.</done>
</task>

</tasks>

<verification>
- Build the project: `cmake --build build`
- Grep confirms reward item storage and Lua bindings exist
- Grep confirms quest log objective text population
</verification>

<success_criteria>
Client quest data layer is complete: reward items stored and Lua-accessible, objective text cached with progress, quest giver icons refresh on all state transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quest-system-polish/02-02-SUMMARY.md`
</output>
