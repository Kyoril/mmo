---
phase: 02-quest-system-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/game_server/objects/game_player_s.cpp
  - src/shared/game_server/objects/game_player_s.h
autonomous: true
requirements:
  - QUEST-05
  - QUEST-06

must_haves:
  truths:
    - "Quest reward spell is granted to player on quest turn-in"
    - "Quest reward triggers fire when quest is rewarded"
    - "Completing a quest in a chain unlocks the next quest via nextchainquestid"
    - "FailQuest sets quest status to Failed and updates client"
  artifacts:
    - path: "src/shared/game_server/objects/game_player_s.cpp"
      provides: "RewardQuest with spell/trigger/chain support, FailQuest implementation"
      contains: "rewardspell"
  key_links:
    - from: "GamePlayerS::RewardQuest"
      to: "spell cast + trigger execution + chain quest unlock"
      via: "CastSpell, ExecuteTrigger, nextchainquestid check"
      pattern: "rewardspell|rewardtriggers|nextchainquestid"
---

<objective>
Complete the server-side quest reward and chain logic by implementing missing reward spell granting, reward trigger firing, chain quest progression (nextchainquestid), and FailQuest.

Purpose: RewardQuest currently handles items, XP, and money but skips rewardspell, rewardtriggers, and nextchainquestid. FailQuest is a stub. These gaps prevent quest chains from working end-to-end and block QUEST-05 and QUEST-06.

Output: Fully functional RewardQuest with all reward types + working FailQuest + chain progression.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/shared/game_server/objects/game_player_s.h
@src/shared/game_server/objects/game_player_s.cpp
@src/shared/proto_data/quests.proto
@src/world_server/trigger_handler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete RewardQuest with spell, triggers, and chain progression</name>
  <files>src/shared/game_server/objects/game_player_s.cpp</files>
  <read_first>
    - src/shared/game_server/objects/game_player_s.cpp (read RewardQuest method around line 690-870)
    - src/shared/proto_data/quests.proto (fields: rewardspell, rewardspellcast, rewardtriggers, nextchainquestid)
    - src/shared/game_server/objects/game_player_s.h (LearnSpell or AddSpell signature if exists)
  </read_first>
  <action>
In `GamePlayerS::RewardQuest`, after the money reward block (around line 845 `Set<uint32>(object_fields::Money, ...)`), add three new blocks:

1. **Reward spell** — If `entry->rewardspell()` is nonzero, look up the spell via `GetProject().spells.getById(entry->rewardspell())`. If found, teach it to the player using `AddSpell(*spell)` (or the equivalent method that exists for learning spells — check game_unit_s.h for `AddSpell` or `LearnSpell`). If `entry->rewardspellcast()` is nonzero instead, cast that spell on the player using `CastSpell` with self-targeting `SpellTargetMap`.

2. **Reward triggers** — Iterate `entry->rewardtriggers()`. For each trigger ID, look it up via `GetProject().triggers.getById(triggerId)`. If found, create a `TriggerContext` with `this` as owner and call the trigger handler (follow the pattern used in `RaiseTrigger` — the player should have access to trigger execution through its world instance's trigger handler).

3. **Chain quest progression** — If `entry->nextchainquestid()` is nonzero, no explicit action is needed beyond ensuring the quest is marked as `Rewarded` in `m_rewardedQuestIds` (which already happens). The chain works because `GetQuestStatus` checks `prevquestid` against rewarded quests. Verify this is correct by confirming `m_rewardedQuestIds.insert(entry->id())` happens before the function returns (it does, around line 860).

Follow existing code style: Allman braces, tab indentation, Doxygen comments for any new helper methods.
  </action>
  <verify>
    <automated>grep -n "rewardspell\|rewardtrigger\|nextchainquest" src/shared/game_server/objects/game_player_s.cpp</automated>
  </verify>
  <acceptance_criteria>
    - game_player_s.cpp RewardQuest contains `entry->rewardspell()` check with spell granting
    - game_player_s.cpp RewardQuest contains iteration over `entry->rewardtriggers()`
    - game_player_s.cpp RewardQuest has `m_rewardedQuestIds.insert(entry->id())` before any chain quest check
    - No compilation errors in game_player_s.cpp
  </acceptance_criteria>
  <done>RewardQuest grants reward spells, fires reward triggers, and chain quest IDs are recorded for GetQuestStatus prevquestid checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FailQuest</name>
  <files>src/shared/game_server/objects/game_player_s.cpp</files>
  <read_first>
    - src/shared/game_server/objects/game_player_s.cpp (read FailQuest stub and AbandonQuest for pattern reference, around line 685 and 596)
    - src/shared/game/quest.h (quest_status enum — specifically quest_status::Failed)
  </read_first>
  <action>
Replace the `FailQuest` TODO stub with a real implementation. Follow the pattern of `AbandonQuest` but set status to `quest_status::Failed` instead of removing the quest:

1. Iterate quest log slots (0 to MaxQuestLogSize).
2. Find the slot where `questLogField.questId == quest`.
3. Look up in `m_quests` map — if not found or status is not `Incomplete`, return false.
4. Set `it->second.status = quest_status::Failed`.
5. Update the `QuestField` in the object fields: `field.status = quest_status::Failed`.
6. Call `Set<QuestField>(...)` to update the field.
7. Notify watcher: `m_netPlayerWatcher->OnQuestDataChanged(quest, it->second)`.
8. Fire fail triggers: iterate `entry->failtriggers()` and execute each (same pattern as reward triggers from Task 1).
9. Return true.

Add Doxygen comment: `/// @brief Fails the specified quest, updating status and firing fail triggers.`
  </action>
  <verify>
    <automated>grep -n "FailQuest" src/shared/game_server/objects/game_player_s.cpp | head -10</automated>
  </verify>
  <acceptance_criteria>
    - game_player_s.cpp FailQuest is no longer a TODO stub
    - FailQuest sets quest_status::Failed on the quest log field
    - FailQuest calls OnQuestDataChanged to notify the client
    - FailQuest iterates failtriggers and executes them
  </acceptance_criteria>
  <done>FailQuest sets status to Failed, updates client via watcher, and fires fail triggers.</done>
</task>

</tasks>

<verification>
- Build the project: `cmake --build build`
- Grep confirms reward spell/trigger/chain handling exists in RewardQuest
- Grep confirms FailQuest is implemented (no TODO)
</verification>

<success_criteria>
RewardQuest handles all proto reward types (items, XP, money, spells, triggers) and quest chains progress via nextchainquestid. FailQuest is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quest-system-polish/02-01-SUMMARY.md`
</output>
