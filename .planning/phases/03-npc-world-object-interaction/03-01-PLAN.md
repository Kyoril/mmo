---
phase: 03-npc-world-object-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/client/Interface/GameUI/TrainerFrame.lua
  - data/client/Interface/GameUI/QuestFrame.lua
  - src/mmo_client/systems/quest_client.cpp
  - src/mmo_client/game_script.cpp
autonomous: true
requirements:
  - NPC-01
  - NPC-02
  - NPC-03
  - NPC-04

must_haves:
  truths:
    - "Trainer spell list refreshes when player buys a spell (TRAINER_UPDATE fires and list re-renders)"
    - "Trainer spell list refreshes when player gains money (MONEY_CHANGED fires and buy button state updates)"
    - "Gossip dialog shows the interacted NPC's name in the frame title when QUEST_GREETING fires"
    - "Gossip action buttons appear and clicking them routes to vendor/trainer/quest interfaces"
    - "Vendor buy (right-click on item) works; sell (click inventory item while vendor is open) works"
    - "Pure gossip menus (showQuests=false) additionally fire GOSSIP_SHOW Lua event alongside QUEST_GREETING"
    - "Closing a gossip dialog fires GOSSIP_CLOSED Lua event"
  artifacts:
    - path: "data/client/Interface/GameUI/TrainerFrame.lua"
      provides: "TrainerFrame Lua UI with working TRAINER_UPDATE refresh"
      contains: "TrainerList_Update(TrainerFrame)"
    - path: "data/client/Interface/GameUI/QuestFrame.lua"
      provides: "Gossip dialog with NPC name in header"
      contains: "GetUnit(\"target\"):GetName()"
    - path: "src/mmo_client/systems/quest_client.cpp"
      provides: "GOSSIP_SHOW and GOSSIP_CLOSED Lua events"
      contains: "TriggerLuaEvent(\"GOSSIP_SHOW\")"
  key_links:
    - from: "trainer_client.cpp TriggerLuaEvent(TRAINER_UPDATE)"
      to: "TrainerFrame_OnTrainerUpdate → TrainerList_Update(TrainerFrame)"
      via: "TRAINER_UPDATE Lua event"
      pattern: "TrainerList_Update(TrainerFrame)"
    - from: "QuestClient::OnGossipMenu TriggerLuaEvent(QUEST_GREETING)"
      to: "QuestFrame_OnQuestGreeting → sets NPC name in title"
      via: "QUEST_GREETING Lua event"
      pattern: "GetUnit.*target.*GetName"
    - from: "QuestClient::OnGossipMenu (showQuests==false)"
      to: "GOSSIP_SHOW Lua event"
      via: "TriggerLuaEvent(\"GOSSIP_SHOW\") after QUEST_GREETING, gated on !showQuests"
      pattern: "TriggerLuaEvent.*GOSSIP_SHOW"
---

<objective>
Audit and fix gaps in the Lua UI frames for Trainer (TrainerFrame.lua) and Gossip (QuestFrame.lua). VendorFrame.lua is already complete — verify it, no changes expected. Also: fire GOSSIP_SHOW/GOSSIP_CLOSED events from quest_client.cpp (locked decision) and verify the vendor sell Lua binding exists in game_script.cpp.

Purpose: NPC-01..04 require working vendor, trainer, and gossip UIs. Research confirmed all Lua function bindings exist (GetVendorItemInfo, BuyVendorItem, GetNumTrainerSpells, BuyTrainerSpell, GetNumGossipActions, GossipAction etc.) — no new C++ registrations needed for the core path. Three concrete gaps remain: (1) TrainerFrame_OnTrainerUpdate is an empty function body; (2) QuestFrame gossip header never shows the NPC name; (3) GOSSIP_SHOW/GOSSIP_CLOSED events are not fired from quest_client.cpp despite the locked decision.

Output: Fixed TrainerFrame.lua (working TRAINER_UPDATE handler) + QuestFrame.lua (NPC name in gossip header) + quest_client.cpp (GOSSIP_SHOW/GOSSIP_CLOSED events) + sell binding verified (game_script.cpp).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-npc-world-object-interaction/03-CONTEXT.md
@.planning/phases/03-npc-world-object-interaction/03-RESEARCH.md

<interfaces>
<!-- Lua functions registered in C++ that Lua frames may call — all already exist, no new registrations needed -->

From game_script.cpp (trainer bindings, lines ~1148-1152):
  GetNumTrainerSpells()                              → uint32
  GetTrainerSpellInfo(slot)                          → spellId(int32), name(String), icon(String), price(int32), isKnown(bool)
  BuyTrainerSpell(slot: uint32)                      → void

From game_script.cpp (vendor bindings, lines ~1142-1145):
  GetVendorNumItems()                                → int32  (also in vendor_client.cpp)
  GetVendorItemInfo(slot)                            → item, icon, price, quantity, numAvailable, isUsable
  BuyVendorItem(slot: uint32)                        → void   (also in vendor_client.cpp)
  CloseVendor()                                      → void   (also in vendor_client.cpp)

From quest_client.cpp (gossip bindings, lines ~99-115):
  GetGreetingText()                                  → String
  GetNumGossipActions()                              → int32
  GetGossipAction(index)                             → {id, text, icon}
  GossipAction(index)                                → void   (calls ExecuteGossipAction)

From trainer_client.cpp — C++ events fired:
  TRAINER_SHOW     (trainer opened — fires when trainer list received)
  TRAINER_UPDATE   (trainer state changed — fired after BuyTrainerSpell succeeds or fails)
  TRAINER_CLOSED   (trainer closed)
  TRAINER_BUY_ERROR  (purchase failed — arg: error code)
  TRAINER_BUY_SUCCEEDED (purchase succeeded)
  MONEY_CHANGED    (gold amount changed)
  SPELL_LEARNED    (new spell added to player's book)
  PLAYER_LEVEL_CHANGED (player leveled up)

From vendor_client.cpp — C++ events fired:
  VENDOR_SHOW      (vendor opened)
  VENDOR_CLOSED    (vendor closed)

From quest_client.cpp — C++ events fired:
  QUEST_GREETING   (fires for BOTH gossip menus AND quest givers — shared event)

Lua UI helpers:
  ShowUIPanel(frame)                → show with panel tracking
  HideUIPanel(frame)                → hide with panel tracking
  GetUnit("target")                 → unit table with :GetName(), :GetLevel() methods
  SidePanel_OnLoad(self)            → initializes close button + title bar
  RefreshMoneyFrame(name, amount, …)→ display gold/silver/copper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TrainerFrame_OnTrainerUpdate + verify TrainerFrame completeness</name>
  <files>data/client/Interface/GameUI/TrainerFrame.lua</files>

  <read_first>
    - data/client/Interface/GameUI/TrainerFrame.lua  ← read full file before any edit; understand TrainerList_Update(self) signature and all registered events
    - src/mmo_client/systems/trainer_client.cpp      ← verify which events are fired (TRAINER_UPDATE, TRAINER_BUY_ERROR, TRAINER_BUY_SUCCEEDED) and when
    - src/mmo_client/game_script.cpp                 ← check for SellItem/SellVendorItem binding and GetTrainerTitle binding; see BuyVendorItem + GetTrainerSpellInfo patterns for reference
  </read_first>

  <action>
**Problem:** `TrainerFrame_OnTrainerUpdate(self)` is an empty function body. When the server sends a TRAINER_UPDATE event (e.g., after BuyTrainerSpell succeeds or fails), the trainer list does not refresh and the buy button state does not update. The player sees a stale list.

**Fix — fill in the empty handler:**
```lua
function TrainerFrame_OnTrainerUpdate(self)
    TrainerList_Update(TrainerFrame);
end
```
Note: call `TrainerList_Update(TrainerFrame)` explicitly with `TrainerFrame` as argument, NOT `TrainerList_Update(self)`. The reason: when TRAINER_UPDATE fires, `self` is the TrainerFrame — which is correct in that context. However, `TrainerList_Update` is also registered directly as an event handler for MONEY_CHANGED, SPELL_LEARNED, PLAYER_LEVEL_CHANGED (lines 117-119), where `self` is also TrainerFrame. Using `TrainerFrame` explicitly is the safe, unambiguous choice that avoids nil errors if TrainerFrame_OnTrainerUpdate is ever called directly.

**Verify completeness while reading:**
- Confirm `TrainerFrame_OnLoad` registers: TRAINER_SHOW → TrainerFrame_OnTrainerShow, TRAINER_UPDATE → TrainerFrame_OnTrainerUpdate, TRAINER_CLOSED → TrainerFrame_OnTrainerClosed, MONEY_CHANGED → TrainerList_Update, SPELL_LEARNED → TrainerList_Update, PLAYER_LEVEL_CHANGED → TrainerList_Update
- Confirm `TrainerList_Update(self)` sets the title bar via `self:GetChild(0):SetText(target:GetName())`
- Confirm `TrainerSpellButton_OnClick` calls `GetTrainerSpellInfo(item.id - 1)` and displays name/icon/cost
- Confirm `TrainerBuyButton_OnClick` calls `BuyTrainerSpell(TrainerFrame.selectedSpellIndex)`
- Known-spell greying: confirmed if `is_known` sets TextColorNormal to "FF999999" in TrainerList_Update
- If any of the above are missing, add them following the existing patterns (see TrainerList_Update for color-coding pattern)
- Do NOT add skill requirement validation — explicitly deferred to Phase 6 (leave TODO comment if absent)

**Verify sell binding (NPC-01):** Open `game_script.cpp` and grep for `SellItem` or `SellVendorItem`. NPC-01 asserts sell is complete — verify it is actually registered. If the grep returns no results, register a sell binding immediately after `BuyVendorItem` following the same lambda pattern, calling the vendor_client sell method. Do NOT stub or skip this — NPC-01 requires buy AND sell.

**Verify trainer title (NPC-02):** Check `TrainerFrame_OnTrainerShow` (or `TrainerList_Update`) to confirm the frame title shows the trainer's configured `title` field (per CONTEXT.md locked decision: "Trainer type reflected in window title using TrainerType enum + title field"). The title must NOT be only `target:GetName()`. Look for `GetTrainerTitle()` or a binding that returns the trainer entry's title string. If absent in `game_script.cpp`, register it following the `GetTrainerSpellInfo` multi-out pattern; then in `TrainerFrame_OnTrainerShow`, call it and set `self:GetChild(0):SetText(trainerTitle)`.

**Code style:** Allman braces if any C-like constructs; Lua `end` on its own line; tabs for indentation.
  </action>

  <verify>
    <automated>
Select-String -Path "data/client/Interface/GameUI/TrainerFrame.lua" -Pattern "TrainerList_Update\(TrainerFrame\)";
Select-String -Path "src/mmo_client/game_script.cpp" -Pattern "SellItem|SellVendorItem"
    </automated>
  </verify>

  <acceptance_criteria>
    - TrainerFrame.lua contains `TrainerList_Update(TrainerFrame)` inside `TrainerFrame_OnTrainerUpdate`
    - `TrainerFrame_OnTrainerUpdate` body is non-empty (confirmed by grep showing at least one statement between `function` and `end`)
    - `TrainerFrame_OnLoad` registers TRAINER_SHOW, TRAINER_UPDATE, TRAINER_CLOSED, MONEY_CHANGED, SPELL_LEARNED, PLAYER_LEVEL_CHANGED events
    - `TrainerBuyButton_OnClick` calls `BuyTrainerSpell(TrainerFrame.selectedSpellIndex)`
    - `TrainerList_Update` sets `self:GetChild(0):SetText(target:GetName())` (NPC name in title)
    - No skill requirement validation code added (remains deferred to Phase 6)
    - game_script.cpp contains `SellItem` or `SellVendorItem` Lua binding (NPC-01 sell path verified)
    - TrainerFrame.lua `TrainerFrame_OnTrainerShow` sets frame title from trainer entry's `title` field (not only `target:GetName()`), using `GetTrainerTitle` or equivalent — per CONTEXT.md locked decision "TrainerType enum + title field"
  </acceptance_criteria>

  <done>TRAINER_UPDATE event causes the trainer spell list to fully re-render including buy button state updates, spell color-coding, and NPC name in title bar.</done>
</task>

<task type="auto">
  <name>Task 2: Add NPC name to QuestFrame gossip header + verify gossip routing</name>
  <files>data/client/Interface/GameUI/QuestFrame.lua</files>

  <read_first>
    - data/client/Interface/GameUI/QuestFrame.lua        ← read full file before any edit; understand QuestFrame_OnQuestGreeting structure and QuestFrame:GetChild(0) title bar usage
    - data/client/Interface/GameUI/VendorFrame.lua        ← reference: line with `self:GetChild(0):SetText(target:GetName())` — the pattern to replicate for NPC name
    - src/mmo_client/systems/quest_client.cpp            ← verify QUEST_GREETING fires for both gossip-only menus and quest givers (line ~545)
  </read_first>

  <action>
**Context locked (from CONTEXT.md):** "Show NPC name/portrait header in gossip dialog."

**Problem:** `QuestFrame_OnQuestGreeting(self)` sets `GreetingText` but never updates the frame title bar (the QuestFrame title stays "QUESTS" — set in `QuestFrame_OnLoad: self:GetChild(0):SetText(Localize("QUESTS"))`). When gossip opens on a vendor NPC, the title should show the NPC's name, not "QUESTS".

**Fix — at the top of `QuestFrame_OnQuestGreeting(self)`, add NPC name display:**
```lua
function QuestFrame_OnQuestGreeting(self)
    -- Show NPC name in title bar (per CONTEXT.md locked decision: NPC name/portrait header)
    local target = GetUnit("target");
    if target then
        self:GetChild(0):SetText(target:GetName());
    end

    -- ... rest of existing function body unchanged (ShowUIPanel, GreetingText, quest buttons, gossip buttons) ...
end
```

Add the target name block as the **first statements** inside `QuestFrame_OnQuestGreeting`, before the `ShowUIPanel(QuestFrame)` call.

**Verify gossip routing while reading:**
- Confirm `GossipActionButton_OnClick(self, mouseButton)` calls `GossipAction(self.id)` — this is the path that triggers gossip actions routing to vendor/trainer
- Confirm gossip action buttons are populated in `QuestFrame_OnQuestGreeting` from `GetNumGossipActions()` / `GetGossipAction(i - 1)`
- Confirm `QuestFrame:RegisterEvent("QUEST_GREETING", QuestFrame_OnQuestGreeting)` is present in `QuestFrame_OnLoad`
- Do NOT create a separate GossipFrame — QUEST_GREETING is the single event for both quest givers and gossip-only NPCs. This is correct design (per research pitfall 5).

**Code style:** Lua `end` on its own line; tabs for indentation; comment above new block.
  </action>

  <verify>
    <automated>Select-String -Path "data/client/Interface/GameUI/QuestFrame.lua" -Pattern "GetUnit.*target.*GetName|target:GetName"</automated>
  </verify>

  <acceptance_criteria>
    - QuestFrame.lua contains `target:GetName()` or `GetUnit("target"):GetName()` inside `QuestFrame_OnQuestGreeting`
    - The NPC name assignment appears before the `ShowUIPanel(QuestFrame)` call in `QuestFrame_OnQuestGreeting`
    - `GossipActionButton_OnClick` calls `GossipAction(self.id)` (routing to vendor/trainer/quest)
    - `GossipActionList` is populated from `GetNumGossipActions()` / `GetGossipAction(i - 1)` loop
    - `QuestFrame_OnLoad` registers `QUEST_GREETING` event: `self:RegisterEvent("QUEST_GREETING", QuestFrame_OnQuestGreeting)`
    - No separate GossipFrame created (grep confirms no `GossipFrame` global function definitions in the file)
  </acceptance_criteria>

  <done>When a gossip-capable NPC is clicked and the QUEST_GREETING event fires, the QuestFrame title bar shows the interacted NPC's name, and gossip action buttons appear and correctly route to vendor/trainer/quest interfaces when clicked.</done>
</task>

<task type="auto">
  <name>Task 3: Fire GOSSIP_SHOW and GOSSIP_CLOSED events from quest_client.cpp</name>
  <files>src/mmo_client/systems/quest_client.cpp</files>

  <read_first>
    - src/mmo_client/systems/quest_client.cpp  ← read full file; locate OnGossipMenu (~line 495) where QUEST_GREETING fires and the handler for the GossipComplete packet to understand where to insert the new event calls
  </read_first>

  <action>
**Context locked (from CONTEXT.md):** "Fire Lua events (GOSSIP_SHOW, GOSSIP_CLOSED) following same pattern as VendorClient."

**Resolution (per Research Pitfall 5):** QUEST_GREETING fires for ALL gossip menus (both pure gossip and quest givers). GOSSIP_SHOW fires ADDITIONALLY for pure gossip-only menus (`showQuests == false`). This coexistence is intentional — do NOT remove or replace QUEST_GREETING.

**Step 1 — OnGossipMenu: fire GOSSIP_SHOW for pure gossip**

In `QuestClient::OnGossipMenu`, locate the existing `FrameManager::Get().TriggerLuaEvent("QUEST_GREETING")` call. Immediately after it, add:

```cpp
// Fire GOSSIP_SHOW for pure gossip menus (no quest list)
// Coexists with QUEST_GREETING per Research Pitfall 5 — both events fire for pure gossip.
// Quest givers only receive QUEST_GREETING (showQuests != 0).
if (!showQuests)
{
    FrameManager::Get().TriggerLuaEvent("GOSSIP_SHOW");
}
```

The `showQuests` variable is already read from the packet earlier in `OnGossipMenu` — no new variable needed.

**Step 2 — GossipComplete handler: fire GOSSIP_CLOSED**

Locate the handler in quest_client.cpp that processes the server `GossipComplete` packet (likely `OnGossipComplete` or the method registered for `realm_client_packet::GossipComplete`). At or near the start of its body, add:

```cpp
// Notify Lua that the gossip session has ended
// per CONTEXT.md locked decision: "Fire GOSSIP_CLOSED following VendorClient pattern"
FrameManager::Get().TriggerLuaEvent("GOSSIP_CLOSED");
```

If the GossipComplete handler does not yet exist in quest_client.cpp, create a minimal one that fires GOSSIP_CLOSED and hides the QuestFrame, following the VENDOR_CLOSED pattern in vendor_client.cpp.

**Code style:** Allman braces, tabs for indentation, `FrameManager::Get().TriggerLuaEvent("EVENT_NAME")` call pattern matches the existing QUEST_GREETING call directly above it.
  </action>

  <verify>
    <automated>
Select-String -Path "src/mmo_client/systems/quest_client.cpp" -Pattern 'TriggerLuaEvent.*GOSSIP_SHOW';
Select-String -Path "src/mmo_client/systems/quest_client.cpp" -Pattern 'TriggerLuaEvent.*GOSSIP_CLOSED'
    </automated>
  </verify>

  <acceptance_criteria>
    - quest_client.cpp contains `TriggerLuaEvent("GOSSIP_SHOW")` inside `OnGossipMenu`, gated on `!showQuests` (pure gossip only)
    - quest_client.cpp contains `TriggerLuaEvent("GOSSIP_CLOSED")` in the GossipComplete packet handler
    - `TriggerLuaEvent("QUEST_GREETING")` is NOT removed — GOSSIP_SHOW fires in addition to (not instead of) QUEST_GREETING
    - No separate GossipFrame created or registered (per Pitfall 5: QuestFrame handles all gossip)
  </acceptance_criteria>

  <done>Pure gossip menus fire GOSSIP_SHOW (in addition to QUEST_GREETING). Gossip close fires GOSSIP_CLOSED. Locked decision "Fire GOSSIP_SHOW/GOSSIP_CLOSED following VendorClient pattern" is fully implemented.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. TrainerFrame.lua:
   - `grep -n "TrainerList_Update(TrainerFrame)" data/client/Interface/GameUI/TrainerFrame.lua` — must return at least one match inside `TrainerFrame_OnTrainerUpdate`
   - `grep -n "TrainerFrame_OnTrainerUpdate" data/client/Interface/GameUI/TrainerFrame.lua` — must show non-empty function (not just `function ... end` with nothing between)

2. QuestFrame.lua:
   - `grep -n "GetName" data/client/Interface/GameUI/QuestFrame.lua` — must return a match inside `QuestFrame_OnQuestGreeting`
   - `grep -n "QUEST_GREETING" data/client/Interface/GameUI/QuestFrame.lua` — must show event registration in `QuestFrame_OnLoad`

3. VendorFrame.lua (no changes — verify as-is):
   - `grep -n "BuyVendorItem" data/client/Interface/GameUI/VendorFrame.lua` — must exist (right-click buy path)
   - `grep -n "VENDOR_SHOW" data/client/Interface/GameUI/VendorFrame.lua` — must show RegisterEvent call

4. game_script.cpp sell binding:
   - `Select-String -Path "src/mmo_client/game_script.cpp" -Pattern "SellItem|SellVendorItem"` — must return at least one result

5. quest_client.cpp GOSSIP events:
   - `Select-String -Path "src/mmo_client/systems/quest_client.cpp" -Pattern "GOSSIP_SHOW"` — must exist, gated on !showQuests
   - `Select-String -Path "src/mmo_client/systems/quest_client.cpp" -Pattern "GOSSIP_CLOSED"` — must exist in GossipComplete handler
   - `Select-String -Path "src/mmo_client/systems/quest_client.cpp" -Pattern "QUEST_GREETING"` — must still exist (not removed)
</verification>

<success_criteria>
- TrainerFrame_OnTrainerUpdate calls TrainerList_Update(TrainerFrame) — not empty
- QuestFrame gossip header displays target NPC name when QUEST_GREETING fires
- Gossip actions route to vendor/trainer/quest via GossipAction(self.id)
- game_script.cpp contains SellItem/SellVendorItem Lua binding — NPC-01 sell path verified
- TrainerFrame_OnTrainerShow uses trainer entry title field (not only NPC name) — per CONTEXT.md locked decision
- quest_client.cpp fires GOSSIP_SHOW for pure gossip menus (showQuests==false), coexisting with QUEST_GREETING — per CONTEXT.md locked decision
- quest_client.cpp fires GOSSIP_CLOSED when GossipComplete packet is received — per CONTEXT.md locked decision
- NPC-01 (vendor): VendorFrame buy/sell confirmed complete
- NPC-02 (trainer): TrainerFrame update handler fixed; trainer title verified
- NPC-03 (gossip): QuestFrame gossip header shows NPC name; GOSSIP_SHOW/GOSSIP_CLOSED events fire; action routing confirmed
- NPC-04 (range): Enforced server-side via IsInteractable() — no Lua changes needed
</success_criteria>

<output>
After completion, create `.planning/phases/03-npc-world-object-interaction/03-01-SUMMARY.md`
</output>
