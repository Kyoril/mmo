#include "test_game_unit.h"

// All implementation is inline in the header.
// This .cpp exists so the CMake glob picks up the file
// and the translation unit is linked into the movement_tests target.
